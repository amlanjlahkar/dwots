#!/bin/sh

[ "$(pulsemixer --get-mute)" -eq 1 ] && echo '🔇 muted' && exit

vol="$(pulsemixer --get-volume | cut -d' ' -f1)"

if [ "$vol" -gt "60" ]; then
    icon="🔊"
elif [ "$vol" -lt "20" ]; then
    icon="🔈"
else
    icon="🔉"
fi

printf "%s\n" "Vol: ${vol}%"
