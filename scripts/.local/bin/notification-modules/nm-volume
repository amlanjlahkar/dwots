#!/bin/sh

[ "$(pamixer --get-mute)" = true ] && echo '🔇 muted' && exit

vol="$(pamixer --get-volume)"

if [ "$vol" -gt "60" ]; then
   icon="🔊"
elif [ "$vol" -lt "20" ]; then
	icon="🔈"
else
	icon="🔉"

fi

printf "%s\n" "Volume: ${vol}%"
