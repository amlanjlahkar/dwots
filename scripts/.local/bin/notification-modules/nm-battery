#!/bin/sh
    
CAPACITY=$(cat /sys/class/power_supply/BAT1/capacity)
STATUS=$(cat /sys/class/power_supply/BAT1/status)

blue=$(tput setaf 4)
reset=$(tput sgr0)

[ "${STATUS}" = "Charging" ] && printf "%s\n" "Battery: ${CAPACITY}% (⚡)" || printf "%s\n" "Battery: ${CAPACITY}%"
