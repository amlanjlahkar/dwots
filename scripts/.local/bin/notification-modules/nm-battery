#!/bin/sh
    
capacity="$(cat /sys/class/power_supply/BAT1/capacity)"
stat="$(cat /sys/class/power_supply/BAT1/status)"

[ "${stat}" = "Charging" ] && printf "%s\n" "🔌 ${capacity}%" || printf "%s\n" "Battery: ${capacity}%"

#if [ "${CAPACITY}" -le 50 ]; then
#    aplay /usr/share/sounds/notification-complete.wav ; notify-send -u critical "🔌 Battery low" "connect to charger"
#fi
