# binding universal hotkeys, specially for window managers.

#---Basics---#
# brightness control(using xorgbacklight)
XF86MonBrightnessUp
    xbacklight -inc 5
XF86MonBrightnessDown
    xbacklight -dec 5

# Volume control(using pactl)
XF86AudioRaiseVolume
    pactl set-sink-volume @DEFAULT_SINK@ +5% ; pkill -RTMIN+2 dwmblocks
XF86AudioLowerVolume
    pactl set-sink-volume @DEFAULT_SINK@ -5% ; pkill -RTMIN+2 dwmblocks
XF86AudioMute
    pactl set-sink-mute @DEFAULT_SINK@ toggle ; pkill -RTMIN+2 dwmblocks

#---Programs---#
# loker service
mod4 + shift + Escape
    slock

##---Terminal Specific Programs---##
#mod4 + Return
#    $TERMINAL

mod1 + {Return,p}
    $TERMINAL -e {nnn -aed,htop}

# browser
mod4 + b
    $BROWSER

# ##---Rofi/Dmneu-specific---##
mod1 + {r,e,w}
    rofi -columns 1 -show {run,emoji,window}	# requires the rofi-emoji package
mod1 + c
    clipmenu -i -p 'clipboard'

##---Dunst-specific---##
# close all notifications
ctrl + space
    dunstctl close-all


#---Scripts---#
# usually placed at `$HOME/.local/bin/scripts/`.
# for taking screenshot
Print
    grabscreen

# for setting wallpaper
#mod1 + w
#	wall
mod1 + shift + w
    wall

# for handling sytem events
mod1 + shift + e
    prompt ' Do you really want to log out of current X session? ' 'pkill dwm'
mod1 + shift + r	
    prompt ' Do you want to reboot your PC? ' 'doas reboot now'
mod1 + shift + s
    prompt ' Do you want to shutdown your PC? ' 'doas shutdown now'

# launch an appimage
mod1 + a
    apimg

# play movies
mod1 + m
    movies

# pdfs
mod1 + shift + p
    pdfs

# password prompt
mod1 + shift + b
    rustwarden

# mount prompt
mod1 + shift + m
    mountprompt
