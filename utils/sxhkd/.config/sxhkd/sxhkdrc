# binding universal hotkeys, specially for window managers.

# Basic
# brightness control(using xorgbacklight)
XF86MonBrightnessUp
    xbacklight -inc 5 -fps 30 ; pkill -RTMIN+6 dwmblocks
XF86MonBrightnessDown
    xbacklight -dec 5 -fps 30 ; pkill -RTMIN+6 dwmblocks

# volume control(using pactl)
XF86AudioRaiseVolume
    pactl set-sink-volume @DEFAULT_SINK@ +5% ; pkill -RTMIN+2 dwmblocks
XF86AudioLowerVolume
    pactl set-sink-volume @DEFAULT_SINK@ -5% ; pkill -RTMIN+2 dwmblocks
XF86AudioMute
    pactl set-sink-mute @DEFAULT_SINK@ toggle ; pkill -RTMIN+2 dwmblocks

# Quick Launch
# spawn st with the flags(helpful when a small interface(terminal session) is needed
# to quickly check/do some stuff
#mod4 + shift + t
#    st -c "_tmpterm" -g 50x15

mod4 + Return
    $TERMINAL

mod1 + {Return,p}
    $TERMINAL -e {nnn -aed,htop}

mod4 + b
    $BROWSER

# Rofi/Dmenu
mod1 + {r,e,,w,f}
    rofi -columns 1 -show {run,emoji,filebrowser}	# requires the rofi-emoji package for emoji mode
mod1 + c
    clipmenu -i -p 'clipboard'

# Dunst
# close all notifications
ctrl + space
    dunstctl close-all

# Scripts
# screenshot
Print
    grabscreen

# scratchpad
mod4 + shift + T
    spad

# set wallpaper
mod1 + shift + w
    wall

# launch an appimage
mod1 + a
    apimg

# copy password saved in bitwarden
mod1 + shift + b
    rustwarden

# connect to wifi or bluetooth device
mod1 + shift + c
    btwifi

# search bookmarks or strings using rofi on the  browser
mod1 + s
    ducksearch

# poweroff, reboot, suspend or lock
mod1 + shift + e
    sysev
